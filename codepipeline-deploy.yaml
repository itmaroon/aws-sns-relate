AWSTemplateFormatVersion: '2010-09-09'
Description: >
  CodePipeline for deploying sns-relate-aws stack from GitHub (Tokyo region)
  to Osaka region (ap-northeast-3).

Resources:
  SNSRelatePipelineRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: sns-relate-pipeline-role
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - codepipeline.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AWSCodePipelineFullAccess
        - arn:aws:iam::aws:policy/AWSCodeBuildAdminAccess
        - arn:aws:iam::aws:policy/AWSCloudFormationFullAccess
        - arn:aws:iam::aws:policy/AmazonS3FullAccess

  SNSRelatePipeline:
    Type: AWS::CodePipeline::Pipeline
    Properties:
      RoleArn: !GetAtt SNSRelatePipelineRole.Arn
      Name: sns-relate-deploy-pipeline
      ArtifactStore:
        Type: S3
        Location: !Sub "sns-relate-pipeline-artifacts-${AWS::Region}-${AWS::AccountId}"
      Stages:
        - Name: Source
          Actions:
            - Name: GitHubSource
              ActionTypeId:
                Category: Source
                Owner: AWS
                Provider: CodeStarSourceConnection
                Version: '1'
              OutputArtifacts:
                - Name: SourceOutput
              Configuration:
                ConnectionArn: arn:aws:codestar-connections:ap-northeast-1:071360906030:connection/XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX
                FullRepositoryId: itmaroon/sns-relate-aws
                BranchName: main
        - Name: Build
          Actions:
            - Name: CodeBuild
              ActionTypeId:
                Category: Build
                Owner: AWS
                Provider: CodeBuild
                Version: '1'
              InputArtifacts:
                - Name: SourceOutput
              OutputArtifacts:
                - Name: BuildOutput
              Configuration:
                ProjectName: sns-relate-build
        - Name: Deploy
          Actions:
            - Name: CloudFormationDeploy
              ActionTypeId:
                Category: Deploy
                Owner: AWS
                Provider: CloudFormation
                Version: '1'
              InputArtifacts:
                - Name: BuildOutput
              Configuration:
                ActionMode: CREATE_UPDATE
                StackName: sns-relate-aws
                Capabilities: CAPABILITY_NAMED_IAM
                TemplatePath: BuildOutput::template.master.yaml
                Region: ap-northeast-3   # ← 大阪リージョンにデプロイ！
                RoleArn: !GetAtt SNSRelatePipelineRole.Arn
      RestartExecutionOnUpdate: true

  SNSRelateArtifactBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "sns-relate-pipeline-artifacts-${AWS::Region}-${AWS::AccountId}"
      VersioningConfiguration:
        Status: Enabled

Outputs:
  PipelineName:
    Value: !Ref SNSRelatePipeline
    Description: Name of the created CodePipeline
